# v118 · Full Pact Bearer class + native Keeper Guild integration

Патч поверх v117.

## Основные изменения

1. `classes.html` получил нативную вкладку «Полная глава».
2. Для класса `Носитель Договора` добавлен полный текст v1.5 из исходного HTML без DM-рекомендаций.
3. Прогрессия `Носителя Договора` в `assets/classes-data.js` заменена на полную таблицу 1–20.
4. `keeper-poi-catalog.html` переработан: больше нет iframe и ссылок на самостоятельные HTML как основной способ чтения.
5. Данные Гильдии Хранителей и каталог PoI вынесены в `assets/keeper-data.js` и отображаются нативным рендером `assets/keeper.js` в стиле сайта.

## Исключено из класса

Удалены только DM-рекомендации: блоки `details.dm-note`, `section#dm-appendix`, DM-лейбл и DM-ссылка в оглавлении. Правила, прогрессия, покровители, якоря, матрицы, списки плетений, дополнительные правила и примеры сохранены.
