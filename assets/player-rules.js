(function(){'use strict';const db=window.WOT_RULES_V14||{chapters:{}},chapters=db.chapters||{},nav=document.getElementById('rules-chapter-nav'),content=document.getElementById('rules-content'),search=document.getElementById('rules-search'),order=Object.keys(chapters),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let active=(location.hash||'').replace('#','');if(!chapters[active])active=order[0];function renderNav(){nav.innerHTML=order.map(key=>'<button type="button" data-chapter="'+key+'" class="'+(key===active?'active':'')+'">'+esc(chapters[key].title)+'</button>').join('');nav.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>{active=b.dataset.chapter;history.replaceState(null,'','#'+active);search.value='';render()}))}function render(){renderNav();content.innerHTML=chapters[active].html;content.dataset.chapter=active}search.addEventListener('input',()=>{const q=search.value.trim().toLocaleLowerCase('ru');content.querySelectorAll('p,li,tr,h2,h3,h4,h5').forEach(el=>el.hidden=!!q&&!el.textContent.toLocaleLowerCase('ru').includes(q))});render()})();
